{
  "name": "@balance/sync-client",
  "version": "0.1.0",
  "private": true,
  "author": "Dmytro Klymenko",
  "license": "PolyForm-Noncommercial-1.0.0",
  "repository": "https://github.com/dklymenko/balance.git",
  "description": "Device-side sync engine for Balance replicas: outbox, single-flight execution, last-write-wins application, and enrollment.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "dependencies": {
    "@balance/core": "*"
  },
  "peerDependencies": {
    "drizzle-orm": ">=0.44"
  },
  "scripts": {
    "test": "vitest run",
    "build": "rm -rf dist && tsc -p tsconfig.json"
  },
  "devDependencies": {
    "better-sqlite3": "^12.11.1",
    "@types/better-sqlite3": "^7.6.13",
    "vitest": "4.1.11"
  },
  "files": [
    "dist"
  ]
}
